######################################################
# Error-Messages for the programmer.
# This messages aren't see by the user.
#
# [ATTENTION]:
# The syntax of the message-templates is
# comparable to the template-syntax for the logger.
#
######################################################

STATIC_FIELD_FROM_NON_STATIC_BLOCK = call static field from non-static method. field == ${field}, method == ${method}.